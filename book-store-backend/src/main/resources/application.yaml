server:
  port: 8080
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/book_store
    username: root
    password: Silvermine1@
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
      database: mysql
      database-platform: org.hibernate.dialect.MySQLDialect
